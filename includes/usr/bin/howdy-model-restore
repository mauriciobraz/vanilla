#!/bin/bash

if ! op --version &> /dev/null; then
	exit 1
fi

if ! op whoami &> /dev/null; then
	exit 1
fi

mkdir -p /etc/local/howdy/models/

if op document get "Howdy Model" --out-file /etc/local/howdy/models/vesper.dat; then
	systemctl --no-block daemon-reload
	exit 0
else
	exit 1
fi
